# Spec A — Quotes and Customers

## Requirements

The sales user opens the quote detail page at `/quotes/[id]` and reviews the line items.
Then they navigate to `/customers` to cross-check the buyer.

Other unrelated text should not match: foo/bar, http://example.com, https://example.com.
