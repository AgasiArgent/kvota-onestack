# Testing 2 → ответ разработки (22.05.2026)

Перед перепроверкой — **`Ctrl+Shift+R`** (Hard Refresh), иначе бандл старый.

## ✅ Починили и задеплоили (перепроверь)

| Row | Где | Что |
|---|---|---|
| 32 | `/messages` — загрузка файла | `.xlsm` / `.xlsb` / `.docm` (макро-Excel/Word) теперь принимаются. Бакет в Supabase дополнен — заодно `.pptx`, `.ods`, `.odt` |
| 40 | `/workspace/logistics` — Исполнитель | Если у юзера нет ФИО в `user_profiles`, теперь короткая часть e-mail до `@` (типа `ekaterina.kravtsova`), а не полный адрес. Сам e-mail остаётся в подсказке при наведении. **Я проверил на проде** — у всех текущих юзеров ФИО заполнено, видно «Алейна Логистик», «Далелова Милана» и т.п. |
| 41 | `/workspace/customs` — Исполнитель | То же что row 40 |
| 47 | `/quotes/{id}?step=calculation` — Наценка | Минимум 5% блокируется на уровне формы. **Я проверил на проде** — ввёл «3» в поле, браузер выдал «Значение должно быть больше или равно 5», под полем подпись «Минимальная наценка — 5%» |
| 58 | `/quotes/{id}` — редактор маршрута | PR #186 (18 мая, commit `fb84a056`) — при правке ячейки страница больше не рефрешится. Тесты пиннят контракт, но я не смог нагрузочно проверить в браузере — попробуй сам после Hard Refresh |

## 🔄 Старый фикс не сработал — открываем заново

| Row | Где | Статус |
|---|---|---|
| **31** | `/quotes/{id}?step=sales` — выпадающий список Ед. | **ПОДТВЕРЖДЕНО, БАГ ЕСТЬ.** Я открыл dropdown — список генерится в DOM (все 13 вариантов), но контейнер рендерится 62×2 px, ты видишь пустоту. Прошлый фикс (PR #179) на Handsontable v15 не сработал. Сделаю отдельной задачей — нужен новый подход. Прости, что в прошлой записке отметили как done |

## 📋 По ФИО (rows 40/41)

Часть фикса — это data. Дозаполним ФИО в `kvota.user_profiles` для тех сотрудников, у кого пусто. Сделаем отдельной задачей. В worst case будешь видеть префикс e-mail вместо полного адреса.

## 🚫 Не баг (правки не нужно)

- **Row 19** (повторная отметка 21.05) — разделение таблицы КП «Требует действия / Остальные» намеренно. UX-приоритизация, не меняем без отдельного product-решения. См. предыдущую записку 14.05.

## 📌 Что ещё в работе (batches 22B/C)

Пока не трогали:

- Row 42 (сумма КП на этапе расчёта на `/quotes`) — нужно уточнение
- Row 57 (не переходит этап после закупки) — на воспроизведение
- Rows 36 / 45 / 46 / 48 / 61 — переработка калькулятора и Контрольного списка
- Rows 62 / 63 (скролл вверх при «Назначить»)
- Rows 64–66 (фильтры на очередях / канбане)
- Rows 67 / 68 / 69 / 70 / 71 / 73 / 74 / 75 (закупки/таможня — функционал)
- Rows 76 / 78 (МОЛ доступ к распределённой КП)
- Row 79 (даты участников распределения)
- Row 77 (удаление локаций — гард использования)
- Row 38 (новая роль «Новичок» + чистка ролей)
- Row 37 (удаление клиентов без ИНН — data task)
- Row 31 (см. выше — открыт заново)

Если что-то срочнее остального — отметь в Testing 2 «PRIORITY» в комментарии, перетасуем.

—
Спасибо 💪
