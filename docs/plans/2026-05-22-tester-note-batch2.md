# Testing 2 → ответ разработки, batch 22B (22.05.2026, вечер)

Перед перепроверкой — **`Ctrl+Shift+R`**.

## ✅ Починили и задеплоили — перепроверь

| Row | Где | Что | Verified мной на проде |
|---|---|---|---|
| 57 | `/quotes/{id}?step=procurement` — кнопка «Завершить закупку» | Роль СтМОЗ (`procurement_senior`) была не в allowlist на API → 403 + застрявший статус. Добавили в три места (`api/invoices.py`, `services/workflow_service.py`). Теперь СтМОЗ закрывает закупку и этап улетает в «Логистика и таможня». | tests 113/113, прод не залогинился под СтМОЗ |
| 62 + 63 | `/workspace/logistics` и `/workspace/customs` — кнопка «Назначить»/«Переназначить» | `autoFocus` на поиске в поповере хватал фокус на (0,0) ДО позиционирования флоатинга → браузер сам скроллил наверх. Заменили на `initialFocus` с `preventScroll`. | да — скроллил до y=800, кликал «Переназначить», y остался 800 |
| 76 + 78 | МОЛ ↔ распределённые КП | `getAssignedQuoteIds()` для МОЛ читал только legacy-колонку `quotes.assigned_logistics_user`. С переходом на per-invoice раздачу колонка стала NULL → /quotes пуст, прямой URL → 404. Теперь делаем UNION с `invoices.assigned_logistics_user`. | да — Милана теперь видит «Всего: 2» (Q-202605-0018, Q-202604-0005), детальная страница открывается |
| 79 | `/quotes/{id}` (Q-202605-0021) → Инф. панель → Участники | МОЗ `assigned_at` тянулся из `workflow_transitions` самого МОЗ — у только что назначенного МОЗ их ещё нет. Теперь берём из `status_history` (reason=`auto: all items routed`) — это момент привязки. | да — «МОП Пономарев Антон 20.05, 15:27 / МОЗ Сергеева Анастасия 20.05, 15:30» |

## 🚫 Не баг (после проверки)

- **Row 42** — «Сумма КП на этапе расчета тянется». Проверил на проде: сумма / прибыль показываются ТОЛЬКО для КП в стадиях «Ревью продаж» и «Контроль КП» (т.е. ПОСЛЕ запуска расчёта). На «Закупки», «Логистика и таможня», «Черновик» — прочерки. Это правильное поведение. Если ты имел в виду что-то другое (например, сумма неправильная для конкретной КП) — дай номер КП и скрин.

## 🔄 Открыты (предыдущая партия)

- **Row 31** — выпадающий Ед. на /quotes/{id}?step=sales всё ещё 2 px. Нужен новый подход к Handsontable v15. Подцеплю отдельно.

## 📌 Ещё в работе (batches 22C/D)

Не трогали: rows 36 / 37 / 38 / 43 / 44 / 45 / 46 / 48 / 61 / 64 / 65 / 66 / 67 / 68 / 69 / 70 / 71 / 73 / 74 / 75 / 77.

Если что-то срочнее — пометь «PRIORITY» в Testing 2.

—
Спасибо 💪
