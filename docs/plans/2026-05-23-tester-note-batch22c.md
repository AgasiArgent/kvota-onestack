# Testing 2 → batch 22C (23.05.2026)

Перед перепроверкой — **`Ctrl+Shift+R`**.

## ✅ Починили / задеплоили

| Row | Где | Что | Тип |
|---|---|---|---|
| 37 | `kvota.customers` | Удалили 7 клиентов без ИНН (5× «Новый клиент …», «Тестовый клиент», «ООО Маслозавод» вместе с 2 черновиками КП). Все references по cascade — контакты/звонки/assignees ушли автоматически | SQL data |
| 38 (часть 1) | Лиана Асаева + Алёна Орманжи | Сняли роль `customs`. Их 9 КП в `draft` отвязали (`assigned_customs_user = NULL`) — в очереди таможни они теперь показываются как «без исполнителя», РОЛ переназначит. **Часть 2 (роль «Новичок» с 0 прав)** — отдельно, нужно product-решение по правам | SQL data |
| 68 | `/quotes/{id}?step=procurement` КПП | Позиции при переносе из «позиций заявки» в КПП-таблицу больше не перемешиваются. Раньше `SELECT … WHERE id IN (…)` без ORDER BY в `assignItemsToInvoice` возвращал rows в planner-order — позиции получали `position = startPos + idx` в произвольном порядке. Добавили `.order("position", { ascending: true })` | PR #214 |
| 72 | `/quotes/{id}?step=customs` Таможня | При копировании / прямом вводе значений с запятой (`12,5`) % пошлины / Утильсбор / Акциз / Экосбор больше не теряют дробную часть. Раньше Handsontable копировал rendered-значение в ru-RU локали («12,5»), а `parseFloat("12,5")` останавливался на запятой → 12. Теперь нормализуем `,` → `.` перед `parseFloat` | PR #215 |
| 71 | Таможня + Логистика — панель «Груз и габариты» | Добавили 4 поля, которые были «Данных нет»: **Валюта КПП**, **Стоимость** (Σ purchase_price × quantity в валюте КПП), **Кол-во** (Σ quantity), **Ед.изм.** (агрегация unit из quote_items через invoice_item_coverage). Если в КПП единицы разные — показываем «разные» | PR #216 |

## 📌 Что пока не трогали (batch 22D)

- Row 44 — опция «поставщик везёт за свой счёт» в 1-м сегменте логистики
- Row 67 — на карточке канбана закупок: комментарий + тег Тендер + дата
- Row 75 — кнопка переназначения в канбане закупок (паттерн скопируем из канбана логистики)
- Row 77 — гард удаления/изменения locations при использовании в КП

## 📌 На product-решение (отдельным батчем)

- Row 38 ч.2 — роль «Новичок» (что входит в права? когда назначается?)
- Row 43 — auto vs manual распределение МОЛ/МОТ + источник таймера
- Row 64+65+66 — фильтры на очередях / канбане (какие именно фильтры?)
- Row 70 — XLS upload в КПП-таблицу (формат шаблона / маппинг колонок?)
- Row 73 — упростить типы сертификатов (какие оставить?)
- Row 74 — стадия «пауза» в канбане (где? условия pause/unpause?)

—

Если что-то срочнее — отметь «PRIORITY» в Testing 2.
Спасибо 💪
