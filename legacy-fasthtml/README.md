# Legacy FastHTML Archive

Archived during Phase 6C of the FastAPI migration (2026-04-20 onwards).

## What's here

- `quotes_new.py` — `/quotes/new` GET+POST form + `customer_search_dropdown` helper (superseded by Next.js `create-quote-dialog.tsx`) — archived 2026-04-20 Phase 6C-1
- `customers_new.py` — `/customers/new` GET+POST form + 4 HTMX customer autocomplete endpoints + DaData INN lookup + 3 company search endpoints + dropdown helpers (superseded by Next.js customer creation flow) — archived 2026-04-20 Phase 6C-1
- `procurement_workspace.py` — `/procurement/{id}` GET page + 7 invoice/items HTMX endpoints + `render_invoices_list` helper (superseded by Next.js `/procurement/kanban` and `/quotes/[id]` procurement step) — archived 2026-04-20 Phase 6C-1
- `cities_search.py` — `GET /api/cities/search` HTMX endpoint (superseded by FastAPI `GET /api/geo/cities/search`) — archived 2026-04-20 Phase 6C-1
- `customers.py` — 26 /customers routes (registry, detail with tabs, inline-edit fragments, contacts, warehouses, calls, notes) + 9 render helpers + ORDER_SOURCE constants. Superseded by Next.js `/customers` + children — archived 2026-04-20 Phase 6C-2B-1
- `suppliers.py` — 10 /suppliers routes (registry, create form, detail with general+brands tabs, edit form, deactivate, brand CRUD) + 4 render helpers (`_supplier_brand_row`, `_supplier_brands_tab`, `_supplier_brands_list_partial`, `_supplier_form`). Superseded by Next.js `/suppliers` + children — archived 2026-04-20 Phase 6C-2B-2. NOT including `/supplier-invoices/*` (no Next.js replacement yet, separate archive decision).
- `companies.py` — 15 routes (/companies unified tabs page + /buyer-companies + /seller-companies legacy split: registry redirects, create forms, detail views, edit forms, soft-delete) + 2 render helpers (`_buyer_company_form`, `_seller_company_form`). Superseded by Next.js `/companies` — archived 2026-04-20 Phase 6C-2B-3. NOT including `/api/{buyer-,seller-,}companies/*` (FastAPI, still alive) or `/customer-contracts/*` (separate area).
- `settings_profile.py` — 11 routes (/settings GET+POST, /settings/telegram GET+POST 301 shims, /profile GET+POST, /profile/{user_id} GET+POST admin save + profile view with tabs, /profile/{user_id}/edit-field|update-field|cancel-edit inline-edit HTMX trio) + 1 render helper (`_render_profile_field_display`). Superseded by Next.js `/settings` + `/profile` — archived 2026-04-20 Phase 6C-2B-4. NOT including `/api/{settings,profile}/*` (FastAPI, still alive), `/admin/*` (separate archive decision), or `/telegram/*` top-level (separate archive 6C-2B-6). FastHTML nav entries for `/settings` and `/profile` (main.py lines 2586, 2590, 2795, 2902) left intact — they become dead links post-archive, safe per Caddy cutover.
- `training.py` — 7 routes (/training registry GET, /training/videos HTMX filter, /training/new-form GET + /training/new POST create, /training/{video_id}/edit-form GET + /training/{video_id}/edit POST update, /training/{video_id}/delete DELETE) + 3 render helpers (`_get_embed_url`, `_get_thumbnail_url`, `_render_video_cards`) + 1 module constant (`_PLATFORM_LABELS`). Superseded by Next.js `/training` — archived 2026-04-20 Phase 6C-2B-5. NOT including `services/training_video_service.py` (service layer left alive in `services/`; becomes effectively unused after this archive but not touched per task scope), `training_manager` role infrastructure (sidebar impersonation, ROLE_LABELS_RU entry, `/admin/impersonate` guard — all still alive and unrelated to this archive), or sidebar/nav entries for `/training` (left intact, dead links post-archive, safe per Caddy cutover). No `/api/training/*` FastAPI sub-app exists.
- `approvals_changelog_telegram.py` — 6 routes from 3 independent cross-cutting areas: `/approvals` GET (Top Manager approvals workspace, standalone page between quote-control clusters), `/changelog` GET (timeline of product updates, reads `services/changelog_service`), `/telegram` GET + `/telegram/generate-code` POST + `/telegram/disconnect` POST + `/telegram/status` GET (Telegram pairing page + HTMX fragments) + 1 render helper (`_telegram_status_fragment`). Superseded by Next.js (/approvals → /quotes?status=pending_approval + dashboard widget, /changelog → app.kvotaflow.ru/changelog reading `/api/changelog`, /telegram → /settings/telegram reading `/api/integrations/*`) — archived 2026-04-20 Phase 6C-2B-6. Removed unused `services.telegram_service` imports (`TELEGRAM_BOT_USERNAME`, `get_user_telegram_status`, `request_verification_code`, `unlink_telegram_account`, `send_procurement_invoice_complete_notification`) from main.py; kept `notify_creator_of_return` (still called by `/quote-control/{quote_id}/return-to-control`). NOT including `/api/telegram/webhook` or `/api/integrations/*` (FastAPI, alive), `services/telegram_service.py` or `services/changelog_service.py` (service layers still alive), `/quote-control/*` routes (separate archive decision, 6C-2B-12), or `/admin/*` feedback routes (separate decision). Sidebar/nav entries for `/approvals`, `/changelog`, `/telegram` (main.py lines 2571, 2726, 2727, 2740) left intact, become dead links post-archive, safe per Caddy cutover.
- `currency_invoices.py` — 7 /currency-invoices routes (registry GET, detail GET, edit POST, verify POST, download-docx GET, download-pdf GET, regenerate POST) + 5 scoped helpers (`_ci_get_company_options`, `_ci_current_entity_value`, `_fetch_ci_for_download`, `_resolve_company_details`, `_convert_docx_to_pdf`). Superseded by Next.js currency-invoice flows — archived 2026-04-20 Phase 6C-2B-8. Preserved in main.py: `_ci_segment_badge` (still called by `_render_currency_invoices_section` on /quotes/documents), `_ci_status_badge`, `_resolve_company_name`, `_fetch_items_with_buyer_companies`, `_fetch_enrichment_data`, `_render_currency_invoices_section`, `_finance_currency_invoices_tab_content`. NOT including `/api/currency-invoices/*` (no FastAPI sub-app exists yet; services consumed directly by Next.js and /finance helpers), `/finance/{deal_id}/generate-currency-invoices` (separate archive 6C-2B-10), `/quotes/{quote_id}/export/invoice` (separate area), `services/currency_invoice_service.py` or `services/currency_invoice_export.py` (service layers still alive, consumed by preserved helpers). Sidebar/nav entry "Валютные инвойсы" → /currency-invoices (main.py ~line 2783) left intact, becomes a dead link post-archive, safe per Caddy cutover. One obsolete test class removed: `TestGroupedRegistryStructure` (7 tests targeting the registry handler) deleted from `tests/test_currency_invoices_grouped_registry.py`; `TestCurrencyInvoicesSectionExists` (6 tests targeting the live `_render_currency_invoices_section` helper) kept.
- `dashboard_tasks.py` — LARGE archive (~2,970 LOC). 2 FastHTML `@rt` routes (`/tasks` and `/dashboard`) plus an 18-helper/constant cluster that nothing outside these two routes ever calls: `DASHBOARD_TABS` tab-config constant + 3 tab-filter helpers (`get_dashboard_tabs`, `get_default_dashboard_tab`, `should_show_dashboard_tabs`), `_build_dashboard_tabs_nav` tab-navigation renderer, `_get_role_tasks_sections` (505-LOC role-specific task-cards builder, shared between /tasks and /dashboard overview), `_get_sales_summary_blocks` (sales-manager date-filter cards), 9 `_dashboard_*_content` tab builders (`_overview`, `_procurement` + `_inner` with error boundary, `_logistics`, `_customs`, `_quote_control`, `_spec_control` with HTMX partial support, `_finance`, `_sales`), `_count_user_tasks` aggregate counter, and `_calculate_days_remaining` + `_format_deadline_display` date formatters. Superseded by Next.js `app.kvotaflow.ru/dashboard` and `/tasks` — archived 2026-04-20 Phase 6C-2B-7. Boundary verified by exhaustive grep trace: every one of the 18 helpers has zero external callers (all exclusive to /tasks or /dashboard, or shared only between the two). Six TDD test files pruned as obsolete: `tests/test_spec_control_duplicate.py`, `tests/test_spec_control_redesign.py`, `tests/test_spec_dashboard_columns.py` deleted entirely (all 77 tests targeted `_dashboard_spec_control_content`); `tests/test_design_c6_c7.py` kept only the C7 `/admin` FIO class (removed C6 `_dashboard_procurement_content` error-handling tests); `tests/test_design_tables.py` kept only the M9 `finance_calendar_tab` class (removed M5/M11/M12 dashboard-table tests); `tests/test_quotes_item_level_assignment.py` lost one `test_dashboard_procurement_reader_queries_quote_items` case (retained the live-surface permission, status-reader, and stage-timer tests). NOT including `services/user_profile_service.py`, `services/approval_service.py`, `services/specification_service.py`, `services/deal_service.py` (service layers still alive, consumed by FastAPI + Next.js), any `/api/*` routers, `/quotes` and other downstream routes, or sidebar/nav entries for `/tasks` and `/dashboard` in main.py (left intact, become dead links post-archive, safe per Caddy cutover).
- `locations.py` — 7 /locations routes (registry GET with search/filters, GET+POST create, detail GET, GET+POST edit, POST soft-delete) + 1 render helper (`_location_form` — shared create/edit form renderer). Superseded by Next.js locations management — archived 2026-04-20 Phase 6C-2B-9 (~888 LOC archived). DELETED separately: `/locations/seed` (bootstrap endpoint that seeded default locations via `services.location_service.seed_default_locations` — only useful in fresh-install scenario, not production; user confirmed "удаляем" over archive; handler + `@rt` decorator deleted from main.py entirely, ~41 LOC removed). Main.py: 31,911 → 30,975 LOC, 148 → 140 @rt (−8: 7 archived + 1 deleted). NOT including `services/location_service.py` (service layer still alive, consumed by `services/supplier_service.py`, `services/seller_company_service.py`, tests, and future FastAPI — `seed_default_locations` itself preserved with its test coverage in `tests/test_location_service.py`), `/supplier-invoices/*` (separate area, not yet archived), or `/api/locations/*` (no FastAPI sub-app exists). No FastHTML sidebar/nav entry for `/locations` was ever added, so no dead links to leave intact. No obsolete tests: `tests/test_route_audit_cleanup.py` references only `/api/locations/search/json` (a different, already-removed endpoint) — untouched.
- `calls_documents_contracts.py` — MULTI-AREA archive: 10 routes across 3 independent cluster areas, combined in one file because none has a Next.js replacement at the time of archival (per user directive 2026-04-20) and each area is small enough to co-locate without ambiguity. Areas: (1) `/customer-contracts` — 4 routes (registry GET with search/filters, create form GET, create submit POST, detail GET) powered by `services/customer_contract_service.py`; (2) `/documents` — 5 routes (upload POST with hierarchical invoice/item binding, download GET → 302 signed URL, view GET → 302 inline, HTMX DELETE, HTMX GET list partial) powered by `services/document_service.py`; (3) `/calls` — 1 route (calls journal registry with search + filters). All three areas archived 2026-04-20 Phase 6C-2B-10a (~1,083 LOC removed from main.py; archive file is ~1,007 LOC). Main.py: 30,975 → 29,892 LOC, 140 → 130 @rt (−10). NO exclusive helpers were moved — each area inlines its rendering. Preserved in main.py at the time of 10a: `_documents_section` helper (was SHARED with `/supplier-invoices/{invoice_id}`; subsequently moved to `supplier_invoices.py` archive in Phase 6C-2B-10b when the supplier-invoices routes were themselves archived), `_quote_documents_section` helper (used by `/quotes/{id}/documents`, still alive). Preserved service layers (all alive): `services/customer_contract_service.py` (consumed by `/spec-control` handlers at main.py lines 19500, 20097 which link to `/customer-contracts/new` — those links become dead, safe per Caddy cutover; also covered by `tests/test_customer_contract_service.py`), `services/document_service.py` (consumed by supplier-invoices, quote-documents, spec-control upload, logistics-expenses, and `api/documents.py` FastAPI), `services/call_service.py` (consumed by `/customers/{id}/calls` detail subtab). Preserved FastAPI surface: `/api/documents/{document_id}/download` + `/api/documents/{document_id}` DELETE (`api/documents.py`, alive, covered by `tests/test_api_documents.py`). NOT including `/supplier-invoices/*` (Phase 6C-2B-10b, pending), `/finance/*`/`/deals/*`/`/admin/*` (separate decisions), `/quotes/{quote_id}/chat` + `/quotes/{quote_id}/comments` (Phase 6C-2B-13 quote-detail cluster). Sidebar/nav entry "Журнал звонков" → `/calls` (main.py line 2766) left intact, becomes dead link post-archive, safe per Caddy cutover. No `/customer-contracts` or `/documents` sidebar entries exist. No obsolete tests deleted — all test references are to FastAPI surface (`/api/documents/*`), service layers (`customer_contract_service`, `call_service`, `document_service`), DB tables (`customer_contracts` table name, not the route), or the alive `/quotes/{id}/documents` surface.
- `supplier_invoices.py` — 4 /supplier-invoices routes (registry GET with supplier/status filters + item + document counts, detail GET with info/financial/items/payments/documents sections, payments/new GET form, payments/new POST submit) + 2 helpers (`_invoice_payment_form` — payment create/edit form renderer exclusive to the payment routes; `_documents_section` — generic documents section component, previously SHARED with the /documents + /customer-contracts + /calls area archived in Phase 6C-2B-10a. Post-10a, the only remaining caller was `/supplier-invoices/{invoice_id}` GET, which archives here in 10b — so `_documents_section` moves alongside its last caller). Archived 2026-04-20 Phase 6C-2B-10b (~1,182 LOC removed from main.py; archive file is ~1,295 LOC). Main.py: 29,892 → 28,710 LOC, 130 → 126 @rt (−4). NO Next.js replacement at archive time — supplier-invoice management will be rewritten via Next.js + FastAPI post-cutover; routes unreachable after Caddy cutover proxies kvotaflow.ru → app.kvotaflow.ru. Preserved in main.py: `_quote_documents_section` helper (separate implementation used by `/quotes/{id}/documents`, NOT archived — lives on). Preserved service layers (all alive): `services/supplier_invoice_service.py`, `services/supplier_invoice_payment_service.py`, `services/buyer_company_service.py`, `services/document_service.py`, `services/database.py`, `services/user_service.py` — all consumed by preserved FastAPI surfaces and service-layer tests (`tests/test_supplier_invoice_service.py`, `tests/test_supplier_invoice_payment_service.py`, `tests/test_ui_invoice_payment.py`, `tests/test_invoice_comparison.py`, `tests/test_bug5_invoice_registry.py`, `tests/test_janna_checklist.py`). NOT including `/api/supplier-invoices/*` (no FastAPI sub-app exists yet), `/api/invoices/*` (separate area — procurement invoice download/letter/verify endpoints in `api/routers/invoices.py`, alive), `/finance/*`/`/deals/*`/`/customer-contracts/*` (separate areas; customer-contracts already archived in 10a). Finance-page link "Открыть реестр инвойсов поставщиков →" at main.py ~line 17885 left intact, becomes a dead link post-archive, safe per Caddy cutover. Obsolete tests cleaned: 2 test classes + 1 orphan test removed from `tests/test_document_chain_upd.py` (5 tests total) — `TestSupplierInvoiceDocumentCount` (2 tests: `test_supplier_invoices_table_has_documents_header`, `test_supplier_invoices_imports_count_documents`) targeting the archived registry handler; `TestSupplierInvoiceDetailDocuments` (2 tests: `test_invoice_detail_has_documents_section`, `test_invoice_detail_passes_supplier_invoice_entity_type`) targeting the archived detail handler; plus one additional test inside `TestDocumentChainEdgeCases` (`test_document_chain_supplier_invoice_column_includes_count`) that also targeted the archived registry handler. The other classes in that file (`TestBuildDocumentChain`, `TestDocumentChainRoute`, `TestQuoteDetailDocumentChainTab`, UPD document-type tests + remaining edge-case tests) kept — they target live surfaces (`_build_document_chain` helper, `/quotes/{id}/documents` merged chain section, UPD constants in `services/document_service.py`). Full test run after pruning: 21 passed.
- `finance_lifecycle.py` — LARGE first-half of the /finance cluster, split from its second half because the /admin area sits between them in main.py. 11 @rt routes + 9 exclusive helpers + 3 ERPS config constants (~3,088 LOC removed from main.py; archive file is ~3,241 LOC). Routes: `/deals` (301 redirect to /finance), `/payments/calendar`, `/finance` (main page with workspace/ERPS/payments/invoices/calendar tabs), `/finance/{deal_id}` (301 redirect to /quotes/{quote_id}?tab=finance_main), `/finance/{deal_id}/payments/new` GET form + POST submit (wrong-path GET returns 404 after archive), `/finance/{deal_id}/payments` POST, `/finance/{deal_id}/payments/{item_id}` DELETE, `/finance/{deal_id}/generate-plan-fact` GET preview + POST submit, `/finance/{deal_id}/plan-fact/{item_id}` GET edit-form + POST submit. Tab-renderer helpers exclusive to /finance (moved): `finance_workspace_tab`, `finance_erps_tab`, `finance_payments_tab`, `finance_invoices_tab`, `finance_calendar_tab` (latter called by /payments/calendar too). ERPS config exclusive to finance_erps_tab (moved): `ERPS_COLUMN_GROUPS`, `ERPS_VIEWS`, `ERPS_COMPACT_COLUMNS` constants + `fmt_days_until_payment`, `fmt_remaining_payment_with_percent`, `render_payments_grouped` formatters. Payment-form helper `_payment_registration_form` exclusive to payments/new route (moved). Archived 2026-04-20 Phase 6C-2B-10c1. Main.py: 28,710 → 25,621 LOC, 126 → 115 @rt (−11). Replaced by Next.js `/deals` + `/finance` pages reading `/api/finance/*` + `/api/deals/*` FastAPI routers — unreachable post-Caddy-cutover. 10c2 pending: `/deals/{deal_id}` detail (line 27121+), `/finance/{deal_id}/stages/*`, `/finance/{deal_id}/logistics-expenses/*`, `/finance/{deal_id}/generate-currency-invoices`, /finance HTMX tail (27914-28255 pre-10c1) + the remaining `_finance_*_tab_content`, `_deals_logistics_tab`, `_finance_logistics_expenses_*` helpers, all still alive in main.py post-10c1. NOT including `/admin/*` (pre-10c1 at lines 24232+, separate archive decision), `/api/finance/*`, `/api/deals/*`, `/api/plan-fact/*` (FastAPI, alive), `services/plan_fact_service.py`, `services/deal_service.py`, `services/erps_service.py`, `services/finance_service.py` (service layers alive, consumed by FastAPI + Next.js + preserved helpers). Sidebar/nav entries for `/deals`, `/finance`, `/payments/calendar` in main.py left intact, become dead links post-archive, safe per Caddy cutover. Obsolete tests cleaned: 2 test files deleted entirely — `tests/test_erps_payment_indicators.py` (867 LOC, all 9 classes targeting archived `fmt_days_until_payment`, `fmt_remaining_payment_with_percent`, `finance_erps_tab`) and `tests/test_design_tables.py` (85 LOC, `TestM9PaymentsCalendarTable` targeting archived `finance_calendar_tab`; former M5/M11/M12 dashboard classes were already removed in Phase 6C-2B-7). Surgical pruning of 4 additional files: `tests/test_unified_payments_tab.py` lost 10 classes (TestPaymentsTabExistsInFinanceRoute + 8 more targeting `finance_payments_tab`/`render_payments_grouped`/GET /finance handler, plus TestFinanceRouteAcceptsCustomerFilter at the tail; retained TestGetCustomerDebtSummaryExists + TestGetCustomerDebtSummaryReturnStructure + TestGetCustomerDebtSummaryBehavior + TestDebtSummaryQueryChain targeting alive `services/plan_fact_service.py.get_customer_debt_summary`, plus TestPaymentsSummaryCalculation + TestOverdueDetectionLogic + TestCustomerGroupingLogic pure-logic tests; 1192 → 653 LOC); `tests/test_deal_payments.py` lost 11 classes targeting archived payment routes + `_payment_registration_form` (retained TestPaymentServiceIntegration + TestPaymentDisplayData + TestPaymentEdgeCases targeting alive `_finance_plan_fact_tab_content` + service layer; 967 → 302 LOC); `tests/test_erps_view_fix.py` lost 3 classes (TestERPSViewsFinanceIncludesAuto, TestERPSColumnGroupsAutoExists, TestFinanceViewGroupOrdering) + 2 helper functions (`_extract_erps_views_source`, `_extract_erps_column_groups_source`) targeting the archived ERPS constants (retained TestMigration161* + TestTotalAmountUsdSavedInCalculation + TestOldViewBrokenDataSource targeting SQL migrations 129/139/161 + alive calculation-save logic; 531 → 315 LOC); `tests/test_p27_finance_integration.py` lost TestFinancePageRendersLogistics (Part 1, 3 tests targeting the archived `/finance/{deal_id}` redirect handler; retained Parts 2-7 targeting 10c2-scope routes `/finance/{deal_id}/stages/*` + `/deals/{deal_id}` + `_deals_logistics_tab` that are still alive post-10c1); `tests/test_deal_visibility_fix.py` lost TestDealListQueryFKHints (2 tests targeting the archived deal-list query in `finance_workspace_tab`; retained TestDealDetailQueryFKHints + TestNoBareJoinsInDealsQueries + TestAllDealsQueriesWithJoinsCovered + TestNestedCustomersFKInDealsQueries targeting the alive deal-detail query in `_finance_fetch_deal_data` and the generic all-deals-queries sweep).
- `control_flow.py` — MEGA-B archive: 23 @rt routes across 2 coherent approval/control workflow stage clusters, combined in one file because both areas gate the same workflow (PENDING_QUOTE_CONTROL → PENDING_APPROVAL → APPROVED → spec creation → approved → signed → DEAL_SIGNED) and a single mega-PR reduces archive review overhead (versus 12a + 12b as separate PRs). Areas: (1) `/quote-control` cluster — 13 routes: `/quote-control` redirect, `/quote-control/{quote_id}` detail with 7-item Janna checklist, `/quote-control/{quote_id}/invoice-comparison` HTMX list + `/quote-control/{quote_id}/invoice/{invoice_id}/detail` HTMX split-screen, `/quote-control/{quote_id}/columns` GET+POST + `/quote-control/{quote_id}/columns/preset/{preset_name}` GET, `/quote-control/{quote_id}/return` GET+POST (calls `notify_creator_of_return`), `/quote-control/{quote_id}/request-approval` GET+POST (justification variant B — round-trips via sales), `/quote-control/{quote_id}/approve` GET+POST; (2) `/spec-control` cluster — 10 routes: `/spec-control` redirect, `/spec-control/create/{quote_id}` GET+POST (v3.0 with contract dropdown + signatory + seller-company prefill + auto-numbering), `/spec-control/{spec_id}` GET+POST (latter includes admin `admin_change_status` that creates deal + generates currency invoices + transitions to DEAL_SIGNED when status=signed), `/spec-control/{spec_id}/preview-pdf` GET + `/spec-control/{spec_id}/export-pdf` GET + `/spec-control/{spec_id}/export-docx` GET (beta), `/spec-control/{spec_id}/upload-signed` POST (via `document_service`), `/spec-control/{spec_id}/confirm-signature` POST (primary user path → create deal + initialize logistics stages + generate currency invoices + transition to DEAL_SIGNED). Exclusive helpers archived alongside their callers: 5 Janna-checklist helpers (`calculate_forex_risk_auto`, `check_markup_vs_payment_terms`, `build_vat_zone_info`, `compare_quote_vs_invoice_prices`, `build_janna_checklist`) + 1 exclusive constant (`MIN_MARKUP_RULES`) — all only called by `/quote-control/{quote_id}` detail GET. Archived 2026-04-20 Phase 6C-2B Mega-B (~4,256 LOC removed from main.py: 4,243 routes-block + 13 constants block; archive file is 4,435 LOC). Main.py: 22,203 → 17,951 LOC, 91 → 68 @rt (−23). Preserved in main.py (live, consumed by other alive surfaces): `normalize_country_to_iso`, `resolve_vat_zone`, `EU_COUNTRY_VAT_RATES`, `COUNTRY_NAME_MAP` (used by calculation path + other consumers); `build_calc_table`, `CALC_COLUMNS`, `CALC_PRESET_BASIC`, `CALC_PRESET_FULL`, `get_user_calc_columns`, `save_user_calc_columns` (used by `/quotes/{quote_id}` cost-analysis + preserved flows); `workflow_transition_history`, `workflow_progress_bar`, `workflow_status_badge`, `quote_header`, `quote_detail_tabs`, `STATUS_NAMES`, `WorkflowStatus` (used by `/quotes/{id}` + finance helpers); `_fetch_items_with_buyer_companies`, `_fetch_enrichment_data` (used by currency-invoice generation paths in `/quotes/{id}` + preserved finance-tab helpers). Preserved service layers (all alive): `services/approval_service.py` (only `count_pending_approvals` consumed by sidebar remains — `request_approval` + `get_pending_approvals_for_user` imports removed as unused); `services/specification_service.py` (`validate_quote_items_have_idn_sku` import removed from main.py — service file stays alive, covered by `tests/test_idn_sku_validation.py`); `services/specification_export.py`, `services/contract_spec_export.py`, `services/contract_spec_docx.py` (PDF/DOCX generators — no main.py callers after archive, still used by preserved `/quotes/{id}/export/specification` via `generate_specification_pdf`); `services/currency_invoice_service.py` (`generate_currency_invoices`, `save_currency_invoices` still called by preserved finance-tab helpers); `services/document_service.py` (still called by other `/quotes/{id}` handlers); `services/logistics_service.py.initialize_logistics_stages` (only called from archived code post-archive; service file alive, may be rewired via `/api/deals` post-cutover); `services/telegram_service.py.notify_creator_of_return` (only caller was `/quote-control/{quote_id}/return` POST; **import removed from main.py**, service file stays alive covered by tests + available for future consumers); `services/quote_approval_service.py` — all symbols still consumed by alive `/quotes/*` submit-justification + manager-decision + approve-department handlers. Service-import cleanups in main.py: removed `request_approval`, `get_pending_approvals_for_user` from `services.approval_service` import (kept `count_pending_approvals`); removed entire `from services.specification_service import validate_quote_items_have_idn_sku` line; removed `from services.telegram_service import notify_creator_of_return` + its TELEGRAM BOT comment block (was embedded inline at the end of /quote-control section). NOT including `/quotes/*`, `/procurement/*` (Mega-C scope); `/admin/*`, `/login`, `/logout`, `/`, `/unauthorized` (Mega-D scope); `/quotes/{quote_id}/chat` + `/quotes/{quote_id}/comments` (quote-detail cluster, Mega-C). Sidebar/nav entry "Спецификации" → `/spec-control` (main.py line ~2564) left intact, becomes dead link post-archive, safe per Caddy cutover. Obsolete tests cleaned (5 files deleted entirely, 5 files surgically pruned): DELETED `tests/test_janna_checklist.py` (1,073 LOC — all 5 Janna helpers archived + all integration assertions target archived /quote-control handler), `tests/test_invoice_comparison.py` (607 LOC — all 8 classes target archived /quote-control/{}/invoice-comparison + /quote-control/{}/invoice/{}/detail routes), `tests/test_spec_update_persistence.py` (569 LOC — all 7 classes depend on archived POST /spec-control/{spec_id} handler source), `tests/test_admin_deal_creation.py` (548 LOC — all 7 classes target archived /spec-control admin_change_status handler) — 2,797 LOC deleted. Surgical pruning: `tests/test_vat_zone_mapping.py` lost `TestBuildVatZoneInfo` class (5 tests targeting archived `build_vat_zone_info`; retained `normalize_country_to_iso` + `resolve_vat_zone` test classes targeting live helpers; 297 → 254 LOC); `tests/test_ui_quote_control_checklist.py` lost `TestMainPyIntegration` class (3 tests searching main.py for archived `build_janna_checklist`; retained `TestQuoteInvoicingItem` + `TestQuoteInvoicingSummary` + `TestChecklistLogic` + `TestChecklistItems` dataclass/logic tests; 436 → 399 LOC); `tests/test_idn_sku_validation.py` lost `TestConfirmSignatureHandlerIntegration` + `TestAdminChangeStatusHandlerIntegration` (2 classes targeting archived handler sources; retained `TestValidateQuoteItemsHaveIdnSku` + `TestFunctionExport` targeting live `services/specification_service.py.validate_quote_items_have_idn_sku`; 506 → 235 LOC); `tests/test_spec_creation_save.py` lost `TestSpecDataDictBuilding` + `TestAutoNumbering` + `TestDeliveryDaysFallback` + `TestSourceCodeVerification` + `TestSpecDataColumnAlignment` (5 classes + `post_handler_source` + `create_handler_source` fixtures targeting archived POST /spec-control/create/{quote_id} handler; retained `TestMigration160` + `TestMigration036Bug` targeting migration files; 673 → 258 LOC); `tests/test_route_audit_cleanup.py` lost `TestSpecControlRouteExists` class (5 tests searching main.py for archived /spec-control routes; retained `TestRemovedRoutesAreGone` + `TestKeptRoutesStillExist` + `TestNoOrphanedReferences` since remaining /spec-control sidebar href keeps that test passing; 239 → 198 LOC); `tests/test_inline_update_and_activity_log.py` updated `TestWorkflowTransitionHistoryCallSites` threshold assertions for the 2 archived call sites (4→2 total calls, 2→0 simple calls — updated comment to reflect Mega-B removal of /quote-control/{quote_id} + /spec-control/{spec_id} simple call sites). Test-pruning lines: 2,797 deleted entirely + 42+37+271+415+41+~10 surgical = ~3,613 LOC of obsolete test removed.
- `quote_detail_and_workflow.py` — MEGA-C archive: 38 @rt routes across 3 coherent areas (the main /quotes cluster, the /procurement workspace cluster that gates procurement returns, and the /quotes/{id}/chat + /comments HTMX helpers), combined in a single file because all 38 routes revolve around one quote lifecycle surface (detail view hosts workflow transition forms, exports, chat, documents, versions; /procurement shares the same quote context and mirrors return-to-control; chat/comments live on the detail page). Archived 2026-04-20 Phase 6C-2B Mega-C (~7,456 LOC removed from main.py in 4 extraction ranges; archive file is ~7,712 LOC). Main.py: 17,948 → 10,441 LOC (−7,507 including import-cleanup lines), 68 → 30 @rt (−38). Areas: (1) `/quotes` cluster — 32 routes: `/quotes` registry with summary stage blocks, `/quotes/{id}` GET tabbed detail (summary/overview/finance_*/cost_analysis), 15 workflow-transition POSTs (submit-procurement + sales checklist, submit-quote-control, return-to-control GET+POST, submit-justification GET+POST, manager-decision, approval-return GET+POST, send-to-client, client-change-request, submit-spec-control, client-rejected, approve-department multi-department), items/{item_id} PATCH + items/bulk POST (Handsontable save), inline PATCH (single-field inline edit), cancel POST, edit GET+POST, DELETE, preview GET (HTMX calculation preview), calculate GET+POST (full calc run), documents GET (merged chain + currency invoices), versions GET + versions/{version_num} GET, export/specification GET (PDF), export/invoice GET (PDF), export/validation GET (XLSM). (2) `/procurement` cluster — 4 routes: `/procurement` redirect, `/procurement/{quote_id}/return-to-control` GET+POST, `/procurement/{quote_id}/export` GET (per-brand Excel). (3) Chat + comments — 2 routes: `/quotes/{quote_id}/chat` GET tab, `/quotes/{quote_id}/comments` POST (returns bubble + OOB empty-state swap). Exclusive helpers archived alongside callers (~13 helpers): `version_badge`, `_lookup_deal_for_quote`, `_calculate_quotes_stage_stats` (registry); `_render_summary_tab`, `_sales_action_toolbar`, `_overview_info_subtab`, `_overview_products_subtab` (detail); `render_preview_panel` (preview); `_build_document_chain`, `_render_document_chain_section`, `_render_currency_invoices_section` (documents); `_build_sales_checklist_card` (was exclusive to archived procurement_workspace.py detail route — zero callers remaining in main.py, co-archived here); `_render_comment_bubble`, `_render_chat_tab` (chat/comments). Preserved in main.py (consumed by api/quotes.py FastAPI handler + preserved /quotes/{id}/cost-analysis `@app.get` route + preserved Finance tab helpers): `SUPPLIER_COUNTRY_MAPPING`, `COUNTRY_NAME_MAP`, `EU_COUNTRY_VAT_RATES`, `EU_ISO_CODES`, `normalize_country_to_iso`, `resolve_vat_zone`, `_calc_combined_duty`, `build_calculation_inputs` (api/quotes.py line 92 imports this); `_format_transition_timestamp`, `workflow_status_badge`, `quote_header`, `workflow_progress_bar`, `workflow_transition_history`, `quote_detail_tabs` (used by /cost-analysis + finance helpers); `_ci_segment_badge` (shared with `_finance_currency_invoices_tab_content`); plus the entire Finance tab helper family (`_quote_documents_section`, `_resolve_company_name`, `_ci_status_badge`, `_fetch_items_with_buyer_companies`, `_fetch_enrichment_data`, `_finance_fetch_deal_data`, `_finance_main_tab_content`, `_finance_plan_fact_tab_content`, `_finance_logistics_tab_content`, `_finance_currency_invoices_tab_content`, `_logistics_expenses_total_el`, `_finance_logistics_expenses_tab_content`, `_finance_payment_modal`, `_deals_logistics_tab`). Preserved service layers (all alive): `services/quote_version_service.py` (fully preserved — consumed by api/quotes.py), `services/quote_approval_service.py`, `services/workflow_service.py`, `services/approval_service.py` (count_pending_approvals only), `services/specification_export.py`, `services/invoice_export.py`, `services/export_validation_service.py`, `services/procurement_export.py` (used by archived exports; service files stay alive for FastAPI rewrite), `services/composition_service.py` (consumed by api/quotes.py), `services/document_service.py`, `services/currency_invoice_service.py`, `services/currency_service.py`, `services/cbr_rates_service.py`, `services/seller_company_service.py`, `services/brand_service.py` — all consumed by preserved surfaces or alive FastAPI routers. Service-import cleanups in main.py: trimmed `services.export_data_mapper` (dropped `fetch_export_data`); removed entire `services.specification_export`, `services.invoice_export`, `services.export_validation_service`, `services.procurement_export`, `services.quote_version_service`, `services.composition_service`, `services.quote_approval_service`, `services.currency_service`, `services.cbr_rates_service` import lines (all only consumed inside archived handlers); trimmed `services.role_service` to `get_user_role_codes` (the /admin handlers still use it inline-imported); trimmed `services.workflow_service` to `WorkflowStatus`, `STATUS_NAMES_SHORT`, `get_quote_transition_history` (the status-badge helpers still use them); trimmed `services.document_service` to 6 symbols consumed by preserved `_quote_documents_section` + `_finance_logistics_expenses_tab_content` (dropped 16 symbols that were only consumed inside archived handlers); trimmed `calculation_mapper` (dropped `safe_int`) and `calculation_models` (kept only `QuoteCalculationInput` — return type for preserved `build_calculation_inputs`). Removed unused dead code: `import httpx` + `BACKEND_URL` constant (no callers in preserved main.py), trimmed `from datetime import datetime, date, timedelta, timezone` to just `datetime`, trimmed `from typing import Dict, Any, List, Optional, cast` to just `Dict, Any, List`, removed `import html as html_mod`. NOT including `/admin/*`, `/login`, `/logout`, `/`, `/unauthorized` (Mega-D scope), `@app.get /quotes/{id}/cost-analysis` and `/cost-analysis-json` (preserved — use `@app` not `@rt`), `api/quotes.py` FastAPI calculate handler (alive), `calculation_engine.py`/`calculation_models.py`/`calculation_mapper.py` (locked). Sidebar/nav entries for `/quotes` + `/procurement` left intact post-archive — dead links but safe per Caddy cutover plan (kvotaflow.ru → app.kvotaflow.ru). Obsolete tests cleaned (5 files deleted entirely, 8 files surgically pruned): DELETED `tests/test_bug2_delivery_city.py` (11 tests — all targeting showSaveConfirmation JS in archived /quotes/{id} GET), `tests/test_currency_invoices_grouped_registry.py` (6 tests — all targeting archived `_render_currency_invoices_section`), `tests/test_fix_customer_reload_and_order_source.py` (5 tests — customer change reload script in /quotes/{id}), `tests/test_sales_action_toolbar.py` (23 tests — `_sales_action_toolbar` helper), `tests/test_svodka_tab_redesign.py` (53 tests — Svodka tab redesign), `tests/test_prodazhi_tab_redesign.py` (48 tests — /quotes/{id} detail sub-tabs). Surgical pruning: `tests/test_quotes_sales_filter.py` kept only `TestSidebarQuotesAllRoles` (6 tests against sidebar link — alive; dropped 26 tests targeting archived `/quotes` GET handler signature + filter bar + sales-only detection; 782 → 266 LOC); `tests/test_route_audit_cleanup.py` dropped 2 tests (`test_quotes_items_patch_still_exists`, `test_quotes_items_bulk_still_exists` targeting archived routes); `tests/test_inline_update_and_activity_log.py` dropped 4 classes (`TestInlineUpdateContactPersonClearing`, `TestInlineUpdateCustomerSameValue`, `TestWorkflowTransitionHistoryCallSites`, `TestInlineUpdateValidityDays`, `TestContactPersonDropdownOnQuoteDetail` — all targeting archived `/quotes/{id}/inline` PATCH handler + archived `/quotes/{id}` detail + call-site counts for archived routes; 502→286 LOC); `tests/test_document_chain_upd.py` dropped `TestBuildDocumentChain` (5 tests) + `TestDocumentChainRoute` (3 tests) + `test_build_document_chain_handles_empty_documents` targeting archived helper; retained TestUPDDocumentType + TestUPDMigration + TestQuoteDetailDocumentChainTab + TestDocumentChainEdgeCases passing tests targeting alive `services/document_service.py` + sidebar nav. `tests/test_deal_visibility_fix.py` threshold update: `TestAllDealsQueriesWithJoinsCovered::test_count_deals_queries_with_joins` lowered from 2 to 1 (one deal-with-joins query remains post-archive in `_finance_fetch_deal_data`; `_lookup_deal_for_quote` query went with archive). `tests/test_erps_view_fix.py` dropped `test_total_amount_usd_saved_to_quotes_table` (the calculate-save logic moved to api/quotes.py). `tests/test_logistics_cost_display.py` dropped `TestMainPyLogisticsCostIntegration` (4 tests) + `TestQuoteDetailLogisticsDataFlow` (2 tests) targeting archived /quotes/{id} Itogo block; retained TestCalculateLogisticsTotalFromInvoices + TestFormatMoneyForLogistics + TestInvoiceDataQualityEdgeCases pure-logic tests. `tests/test_main_calc_entry_points.py` updated `EXPECTED_CALL_SITES` from 2 to 0 (both main.py build_calculation_inputs call sites archived; only api/quotes.py caller remains) + renamed `test_main_imports_get_composed_items` to `test_api_quotes_imports_get_composed_items` (import moved). Full test run after pruning: 2507 passed, 57 skipped, 3 xfailed, 18 warnings, 84 errors (all pre-existing DB-connection issues, unrelated to archive; baseline was 2709 passed — delta is 202 tests removed, matching the archive-related test count).
- `auth_shell.py` — MEGA-D archive: 5 @rt routes — the FastHTML auth shell — `/` (root redirect: /tasks if logged in else /login), `/login` GET (login form page) + POST (Supabase anon-client authenticate + session seed + redirect to /dashboard, with error-re-render on failure), `/logout` GET (session.clear() + redirect to /login), `/unauthorized` GET (401/403 error page shown when user lacks required role). NO exclusive helpers moved — all auxiliaries (`require_login`, `page_layout`, `icon`, `get_supabase`, `get_user_role_codes`, `RedirectResponse`) stay alive in main.py because they are consumed by preserved `/admin/*` cluster + FastAPI sub-app + middleware stack. Archived 2026-04-20 Phase 6C-2B Mega-D (~501 LOC removed from main.py; archive file is ~553 LOC). Main.py: 10,441 → 9,940 LOC, 30 → 25 @rt (−5). Replaced by Next.js auth shell at app.kvotaflow.ru/ (Supabase Auth session handshake + /login + /logout + /unauthorized views) — unreachable post-Caddy-cutover. Service-import cleanup: removed `get_anon_client` from the `from services.database import get_supabase, get_anon_client` line (its sole caller was archived POST /login); `get_supabase` stays (consumed everywhere); `get_anon_client` itself stays alive in `services/database.py` for potential reuse. Preserved in main.py post-archive: 25 `/admin/*` @rt routes + their exclusive helpers + all middleware (SessionMiddleware, ApiAuthMiddleware, Sentry) + FastAPI sub-app mount at `/api`. One dead redirect target: main.py line 6362 (preserved `@app.get /quotes/{id}/cost-analysis` handler) redirects to `/unauthorized` on role-check failure — route no longer exists, so that branch now returns 404; acceptable because end-user traffic no longer reaches this Python app post-cutover (kvotaflow.ru → app.kvotaflow.ru via Caddy). NOT including `/admin/*` (separate archive decision post-Mega-D; user to review remaining footprint before choosing to archive or keep as FastHTML-backed admin until post-cutover rewrite), FastAPI auth middleware `api/auth.py` (alive — its `RedirectResponse("/login")` for legacy session fallback still renders correctly at the HTTP layer even though the `/login` route returns 404; affected tests in `tests/test_api_documents.py` assert on the 303 status + location header, not on the target-route registration — they stay green). No test files deleted: no `tests/test_auth_*.py` / `test_login_*.py` / `test_logout_*.py` / `test_unauthorized_*.py` ever existed. `tests/test_api_documents.py` lines 93-99 + 211 still pass (they assert middleware redirect behaviour, not route existence). Auth service-layer tests (`services/auth_service`, session helpers) do not exist in tests/ — all auth surface was handled inline in main.py, not factored to a service module.
- `cost_analysis.py` — MEGA-F archive: 1 `@app.get` route (NOT `@rt` — direct FastHTML app routing) — `/quotes/{quote_id}/cost-analysis` GET, the read-only P&L waterfall dashboard for a quote (aggregates phase_results across items, renders top-line cards + P&L waterfall table with W2-W10 logistics breakdown, gross/net profit, markup %). Visible to finance, top_manager, admin, quote_controller. Archived 2026-04-20 Phase 6C-2B Mega-F (~317 LOC removed from main.py — handler body + section header; archive file is ~335 LOC). Plus 1 deleted stub: `@app.get /quotes/{quote_id}/cost-analysis-json` (7 LOC) was a 501 Not Implemented placeholder that never got built — DELETED entirely (not preserved in archive; no recovery value). Superseded by FastAPI endpoint `GET /api/quotes/{quote_id}/cost-analysis` (api/cost_analysis.py + api/routers/cost_analysis.py — ALIVE) + Next.js page at `frontend/src/app/(app)/quotes/[id]/cost-analysis/page.tsx` (ALIVE, consumes the FastAPI endpoint) — ported in PR #50. This archive removes the last FastHTML-rendered route from main.py; after this merges, main.py contains zero HTML/HTMX routes (0 `@rt` + 0 `@app.get` HTML routes). Unblocks 6C-3 Docker uvicorn switch. Preserved in main.py (consumed by other alive surfaces): `require_login`, `user_has_any_role`, `get_supabase`, `quote_detail_tabs`, `quote_header`, `page_layout`, FastHTML component imports. NOT including `/api/quotes/{id}/cost-analysis` (FastAPI, alive), Next.js page (alive), or calculation engine. Obsolete test deleted entirely: `tests/test_cost_analysis_dashboard.py` (1,544 LOC — 13 test classes regex-parsing main.py for the archived handler: route existence, tab config, handler source, aggregation logic, profit calculations, logistics breakdown, not-calculated message, P&L display, currency formatting, multi-item aggregation, org isolation, section order, tab navigation). The FastAPI replacement has full behavioral coverage in `tests/test_cost_analysis_api.py` (410 LOC — 7 tests: 401 no JWT, 403 no role, 403 wrong org, 404 missing/soft-deleted quote, 200 has_calculation=false when no calc rows, 200 aggregated totals + derived metrics for 2-item quote, markup_pct zero-division safety). Live sidebar/nav entries for `/quotes/{id}/cost-analysis` (in `quote_detail_tabs` tab config) left intact — they'll resolve to the Next.js route via Caddy post-cutover, safe. No other tests touched.
- `admin_cluster.py` — MEGA-E archive: 25 @rt routes — the entire `/admin/*` cluster, spanning 6 areas — (1) `/admin` + `/admin/users` (2 routes: user listing with role badges + Telegram status; `/admin/users` 303-redirects to `/admin`); (2) `/admin/feedback` (4 routes: list with status filter tabs + HTMX status POST + HTMX sync-ClickUp POST + detail with screenshot/debug-context/ClickUp link); (3) `/admin/users/{user_id}/roles*` (5 routes: HTMX edit/update/cancel + standalone page GET + POST — with safety checks: min 1 role, admin cannot remove own admin role); (4) `/admin/brands` (6 routes: registry with stats grid + create form GET+POST + edit form GET+POST + delete POST); (5) `/admin/procurement-groups` (6 routes: same CRUD structure as brands — sales-group→procurement-user routing); (6) `/admin/impersonate` + `/exit` (2 routes: set/clear `session.impersonated_role` — admin OR training_manager can set, exit is unconditional). Archived 2026-04-20 Phase 6C-2B Mega-E (~2,460 LOC removed from main.py; archive file is ~2,741 LOC). Main.py: 9,940 → 7,490 LOC, 25 → 0 @rt — **this is the final FastHTML archive before 6C-3 Docker uvicorn switch**. After this PR merges, main.py hosts zero @rt routes; the next PR replaces main.py with a minimal uvicorn-bootstrap stub serving the FastAPI sub-app (api/app.py). Module-level constants moved alongside the routes (all exclusive to the admin cluster): `STATUS_LABELS` (feedback status → label/badge-css mapping), `FEEDBACK_TYPE_LABELS_RU` (feedback type → Russian label), `VALID_IMPERSONATION_ROLES` (operational-roles frozenset; security invariant: does NOT contain "admin" or "training_manager" — prevents privilege escalation via direct `/admin/impersonate?role=admin` URL). No top-level service imports removed: all admin service imports (`services.brand_service`, `services.route_procurement_assignment_service`, `services.user_profile_service`, `services.feedback_service`, `services.clickup_service`, most `services.role_service` symbols) lived inline inside the archived handlers, so they go out with the archive body. Top-level imports that stay alive post-archive: `services.role_service.get_user_role_codes` (line 37 — consumed by `user_has_any_role` helper at main.py:4364); `services.workflow_service` symbols (consumed by preserved `workflow_status_badge` + `quote_header` helpers); `services.database.get_supabase` (widely consumed); calculation engine imports (consumed by `build_calculation_inputs`); document service imports (consumed by `_quote_documents_section`). Preserved in main.py (dead after admin archive but intentionally left for 6C-3 cleanup): `require_login`, `page_layout`, `icon`, `btn`, `btn_link`, `sidebar`, `main_layout`, `format_date_russian`, `ANNOTATION_EDITOR_JS`, `feedback_modal`, and related auth/nav/UI helpers. The 6C-3 PR will evaluate these en-masse and either relocate (SessionMiddleware, Sentry, ApiAuthMiddleware) or delete (sidebar/nav/page_layout if genuinely unused by any remaining @app.get route). Latent bug preserved (not fixed): `/admin/feedback/{short_id}/sync-clickup` POST handler referenced `logger.error(...)` without importing `logger` — would have raised NameError on exception path. Dead code, not worth fixing; documented in archive docstring. Preserved FastAPI surface: `/api/admin/*` router (`api/routers/admin.py`, alive, covered by `tests/test_api_routers_admin.py`) — handles user-create + role-update + VAT-rate admin endpoints consumed by Next.js. Sidebar entries left intact (dead links post-archive, safe per Caddy cutover): "Обращения" → `/admin/feedback` (main.py ~line 2738) + implicit `/admin` root from `btn_link` references in preserved nav. Replaced by Next.js `/admin` + `/api/admin/*` FastAPI router — unreachable post-Caddy-cutover. Obsolete tests cleaned: DELETED `tests/test_design_c6_c7.py` (167 LOC — the C7 FIO test class targeted the archived `/admin` GET handler source; former C6 dashboard tests were already removed in Phase 6C-2B-7). Surgical pruning: `tests/test_feedback_enhanced.py` lost 5 classes targeting archived /admin/feedback routes (TestAdminFeedbackRoutesExist, TestAdminFeedbackListBehavior, TestAdminFeedbackDetailBehavior, TestStatusUpdateEndpoint, TestFeedbackStatusLabels — all searching main.py for archived routes/constants) + sections 5-8 + 12; retained TestClickUpServiceExists, TestClickUpServiceBehavior, TestTelegramServiceWithPhotoExists, TestTelegramServiceWithPhotoBehavior, TestEnhancedFeedbackPostHandler, TestSidebarFeedbackLink (alive — sidebar still exists), TestAnnotationEditorJS, TestFeedbackEdgeCases (all targeting live api/feedback.py + sidebar + page_layout + services); 1,173 → 891 LOC. `tests/test_training_manager_role.py` lost `TestImpersonationRouteAllowsTrainingManager` (2 tests targeting archived /admin/impersonate route source), `TestAdminNotInValidImpersonationRoles` (3 tests targeting archived VALID_IMPERSONATION_ROLES module constant), and `test_admin_badge_colors_include_training_manager` single test from `TestTrainingManagerEdgeCases` (targeted archived `badge_class` dict in /admin handler); retained `TestRoleLabelsContainTrainingManager` (ROLE_LABELS_RU alive), `TestSidebarImpersonationForTrainingManager`, `TestSidebarFullMenuForTrainingManager`, `TestSidebarHidesAdminSectionForTrainingManager` (all target alive sidebar function), `TestUserHasAnyRoleWithTrainingManager` (4 tests targeting alive user_has_any_role helper), `test_impersonation_dropdown_label_for_training_manager` + `test_training_manager_user_with_dual_roles` (alive sidebar + user_has_any_role); 532 → 439 LOC. No other test files touched (`tests/test_api_routers_admin.py`, `tests/test_api_feedback.py`, `tests/test_brand_supplier_assignment_service.py`, `tests/test_procurement_routing.py` all target FastAPI or service layer — preserved untouched).
- `ops_deal_finance_customs_logistics.py` — MEGA-A archive: 24 @rt routes across 4 coherent post-deal operational-stage lifecycle slices, combined in one file because each slice is independent (no cross-referencing exclusive helpers) and a single mega-PR reduces archive review overhead. Areas: (1) `/deals/{deal_id}` — 1 route (301 redirect to /finance/{deal_id}, which itself was archived in 10c1; legacy-bookmark compat); (2) /finance HTMX tail — 6 routes (`/finance/{deal_id}/stages/{stage_id}/expenses` POST deprecated-redirect, `/finance/{deal_id}/stages/{stage_id}/status` POST, `/finance/{deal_id}/logistics-expenses/new-form` GET, `/finance/{deal_id}/logistics-expenses` POST, `/finance/{deal_id}/logistics-expenses/{expense_id}` DELETE, `/finance/{deal_id}/generate-currency-invoices` POST); (3) /customs cluster — 12 routes (`/customs` redirect, `/customs/declarations` registry, `/customs/declarations/upload` GET+POST, `/customs/declarations/upload/preview` GET, `/customs/declarations/upload/confirm` POST, `/customs/declarations/{declaration_id}/items` GET, `/customs/{quote_id}` GET+POST with Handsontable license columns, `/customs/{quote_id}/items/{item_id}` PATCH legacy per-item update, `/customs/{quote_id}/return-to-control` GET+POST); (4) /logistics cluster — 5 routes (`/logistics` redirect, `/logistics/{quote_id}` GET+POST with invoice-level logistics cards, `/logistics/{quote_id}/return-to-control` GET+POST). Only one exclusive helper moved with its callers: `_finance_logistics_expenses_stage_section` (re-renders stage section for HTMX swap after create/delete; only callers were the archived POST + DELETE on `/finance/{deal_id}/logistics-expenses`). Archived 2026-04-20 Phase 6C-2B-Mega-A (~3,410 LOC removed from main.py; archive file is ~3,612 LOC). Main.py: 25,621 → 22,204 LOC, 115 → 91 @rt (−24). Replaced by Next.js pages + `/api/customs/*`, `/api/deals/*`, `/api/logistics/*`, `/api/finance/*` FastAPI routers — unreachable post-Caddy-cutover. Preserved in main.py (consumed by /quotes/{quote_id} finance tabs that stay alive): `_finance_fetch_deal_data`, `_finance_main_tab_content`, `_finance_plan_fact_tab_content`, `_finance_logistics_tab_content`, `_finance_currency_invoices_tab_content`, `_logistics_expenses_total_el`, `_finance_logistics_expenses_tab_content`, `_finance_payment_modal`, `_deals_logistics_tab` — all called by /quotes/{quote_id} GET at main.py:6124-6160 when tab in {finance_main, plan_fact, logistics_stages, currency_invoices, logistics_expenses}; plus `_ci_segment_badge`, `_ci_status_badge`, `_resolve_company_name`, `_fetch_items_with_buyer_companies`, `_fetch_enrichment_data` used by `/quotes/{id}/documents` + `_finance_currency_invoices_tab_content`. Preserved service layers (all alive): `services/customs_declaration_service.py` (after archive it has no runtime callers in main.py but still covered by `tests/test_customs_declarations.py` and may be rewired via Next.js + /api/customs post-cutover), `services/logistics_service.py`, `services/logistics_expense_service.py`, `services/plan_fact_service.py`, `services/deal_service.py`, `services/workflow_service.py`, `services/currency_invoice_service.py`. Service-import cleanups in main.py: removed `from services.customs_declaration_service import (...)` (7 symbols, all exclusive to archived /customs/declarations routes); trimmed `services.workflow_service` imports (dropped `complete_logistics`, `complete_customs` — no remaining consumers); trimmed `services.logistics_service` top-level imports to the 4 symbols used by preserved helpers (`get_stages_for_deal`, `get_stage_summary`, `stage_allows_expenses`, `STAGE_NAMES` — dropped `update_stage_status`, `get_expenses_for_stage`, `STAGE_CODES`, `STAGE_CATEGORY_MAP`). NOT including `/admin/*` (Mega-B scope), `/quote-control/*`, `/spec-control/*` (Mega-B scope), `/quotes/*`, `/procurement/*` (Mega-C scope), `/api/customs/*`, `/api/deals/*`, `/api/logistics/*`, `/api/finance/*`, `/api/plan-fact/*` (FastAPI, alive), `calculation_engine.py`, `calculation_models.py`, `calculation_mapper.py` (locked). Sidebar/nav entries for `/customs`, `/logistics`, `/deals`, `/finance`, `/payments/calendar` left intact, become dead links post-archive, safe per Caddy cutover. Obsolete tests pruned (surgical, no whole files deleted): `tests/test_bug4_customs_artikul.py` rewritten to keep ONLY `TestHsCodeValidationInWorkflowService` (3 tests targeting `services/workflow_service.complete_customs`); deleted 7 classes targeting archived customs Handsontable + POST handler (TestColHeadersIncludeArtikul, TestColumnsConfigIncludeProductCode, TestBuildCustomsItemIncludesProductCode, TestSaveDoesNotSendProductCode, TestHsCodeValidationOnCompletion, TestProductCodeEdgeCases, TestProductCodeDataFlow); 486 → 91 LOC. `tests/test_customs_page_fixes.py` rewritten to keep TestCalcEngineLicenseIntegration + TestLicenseCostEdgeCases + TestConsistencyChecks (scoped to preserved `build_calculation_inputs` in main.py); deleted TestButtonTextChange + TestRubFootnote + 4 archived-specific tests in TestConsistencyChecks (`test_customs_form_still_exists`, `test_handsontable_section_still_exists`, `test_customs_save_button_has_correct_action_value`, `test_no_duplicate_save_buttons_in_customs`). `tests/test_customs_licenses.py` lost 2 classes (TestHandsontableLicenseColumns 11 tests + TestLicenseDataPreparation 4 tests targeting archived Handsontable columns/save JS; retained TestLicenseMigrationExists + TestBulkUpdateLicenseFields + TestLicenseSummaryStats + TestLicenseValidation + TestLicenseCompletionCheck + TestLicenseEdgeCases targeting migrations, `api/customs.py` bulk handler, and pure logic; 674 → 514 LOC). `tests/test_logistics_routes.py` lost 10 classes targeting archived /logistics & /deals routes (TestDealDetailRouteExists, TestDealDetailTabNavigation, TestLogisticsTabContentRoute, TestAddExpenseRoute, TestAddExpenseToGtdUploadRejected, TestUpdateStageStatusRoute, TestLogisticsRoutesRequireAuth, TestLogisticsRoutesRequireRole, TestLogisticsRoutesOrgIsolation, TestExpenseFormUI; retained TestDealCreationInitializesStages + TestLogisticsServiceFileExists + TestMigrationFilesExist + TestLogisticsCategoriesExist + TestMainPyImportsLogisticsService + TestStageCategoryMapping targeting service layer + migrations; 814 → 363 LOC). `tests/test_p27_finance_integration.py` lost 4 classes targeting archived /finance-stages + /deals/{deal_id} routes (TestFinanceStageStatusRoute, TestFinanceStageExpensesRoute, TestDealsRedirectsToFinance, TestExpenseStatusHandlerRedirects, TestDealsDetailTabLinks; retained TestLogisticsTabFormActions + TestOldDealsLogisticsRoutesRemoved targeting preserved `_deals_logistics_tab` helper; 383 → 191 LOC). `tests/test_design_c1_c2_c3.py` lost `TestC2_DeliveryMethodIcons` (4 tests targeting archived `delivery_method_icon` dict in /logistics/{quote_id} GET; 387 → 345 LOC). `tests/test_inline_update_and_activity_log.py`: two `TestWorkflowTransitionHistoryCallSites` tests updated to lowered thresholds (6→4 total call sites, 4→2 simple calls) to account for two archived workspace pages (/logistics/{quote_id} + /customs/{quote_id}) whose `workflow_transition_history(quote_id)` calls went with the archive. Full test run after pruning: 2,899 passed, 57 skipped, 4 xfailed (same as baseline minus the deleted tests), 84 errors (all pre-existing DB-connection issues, unrelated to archive).

## Why preserve

- Git history alone is harder to browse than a dedicated archive directory
- Routes were broken post-migration-284 (Phase 5d exempt list) before archival — not losing working code
- Allows easy copy-back if an obscure flow needs restoration

## Not imported

These files are NOT imported by `main.py` or `api/app.py`. The FastHTML routes they defined are effectively deleted from the running app. They will return 404 in production, which is an improvement over the 500 errors they returned before this archive.

## Recovery

To restore a route: copy the handler back to `main.py`, restore imports, re-apply the `@rt(...)` decorator. Then regenerate tests if needed. Not recommended — rewrite via Next.js + FastAPI instead.
