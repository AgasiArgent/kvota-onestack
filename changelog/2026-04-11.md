---
title: "Procurement Phase 3 — страна отгрузки, Инкотермс, MOQ, валюты"
date: 2026-04-11
version: "0.6.0"
---

## Новое

- **Страна отгрузки на КП поставщика** — в форме создания КП поставщику появился выбор страны отгрузки (двуязычный список RU+EN, 264 страны). Страна подхватывается автоматически из карточки поставщика и сохраняется как ISO-код. Нужно для автоматического расчёта НДС в следующем релизе
- **Полный список Инкотермс 2020** — везде, где указывались условия поставки, теперь доступны все 11 терминов: EXW, FCA, CPT, CIP, DAP, **DPU**, DDP, FAS, FOB, CFR, CIF (раньше было только 5). Добавлено в создание КП клиенту, создание КП поставщику и форму расчёта
- **Мин. заказ (MOQ) на позициях КП поставщика** — в таблице поставщика появилась колонка «Мин. заказ» между «Кол» и «Цена». Когда количество меньше минимального заказа поставщика, позиция подсвечивается жёлтым с подсказкой, а в шапке показывается счётчик нарушений. Ограничение мягкое — расчёт не блокируется
- **5 новых валют** — к USD/EUR/RUB/CNY/TRY добавились AED (дирхам ОАЭ), KZT (тенге), JPY (йена), GBP (фунт) и CHF (швейцарский франк). Все поддерживаются ЦБ РФ, работают во всех формах расчёта, логистики, КП поставщика и платежей
- **Поиск городов через HERE работает стабильнее** — локальный кэш результатов на 256 записей снижает нагрузку на HERE API, поддержка всех 250 стран ISO вместо 28 жёстко зашитых (пропадала, например, Бразилия и Египет)

## Технические детали

- Новые колонки в `kvota.invoices`: `pickup_country_code CHAR(2)` (ISO-3166 alpha-2) и `supplier_incoterms TEXT` (миграция 266)
- Новая колонка в `kvota.quote_items`: `min_order_quantity NUMERIC` (миграция 267)
- CHECK-ограничение на `kvota.deal_logistics_expenses.currency` смягчено с жёсткого списка из 5 валют до regex `^[A-Z]{3}$` (миграция 268) — теперь добавление новых валют не требует миграции
- Новый общий модуль `frontend/src/shared/ui/geo` с компонентами `CountryCombobox` и `CityCombobox` — используется в создании КП клиенту, КП поставщика и дальше будет в других формах. Список стран строится из `Intl.DisplayNames`, без внешних зависимостей
- Новый JSON-эндпоинт `GET /api/geo/cities/search` — Next.js UI и AI-агенты ходят через него, старый HTMX-эндпоинт `/api/cities/search` остался для FastHTML-страниц
- `services/here_service.py` — замена словаря alpha-3 → alpha-2 на `pycountry` (полное покрытие ISO 3166), manual-LRU кэш вместо `functools.lru_cache` (сохраняет исходный регистр запроса при обращении к HERE)
- Единые константы `INCOTERMS_2020` и `SUPPORTED_CURRENCIES` в `frontend/src/shared/lib/` — устранили три разных захардкоженных списка
- 122 новых Python-теста + 179 фронтенд-тестов, все зелёные
