BROWSER TEST
timestamp: 2026-02-10T17:50:00+03:00
session: 2026-02-10 #1
base_url: https://kvotaflow.ru

TASK: [P2.6+P2.10] Document chain tab + УПД document type support
URL: /quotes/{any_existing_quote_uuid}

TEST 1: Document chain tab on quote detail
STEPS:
1. Navigate to any existing quote detail page (e.g., one from the /quotes list)
2. Look for a "Цепочка документов" (Document Chain) tab among the quote tabs
3. Click the "Цепочка документов" tab
4. Verify the tab loads and shows document sections grouped by type:
   - КП (quote documents)
   - Спецификация (specification documents)
   - Инвойс (invoice documents)
   - ГТД (customs declarations)
   - УПД (universal transfer documents)
5. Each section should show document count if documents exist
6. If no documents exist for a type, the section should still be visible but empty or show "Нет документов"
7. Check console for errors

TEST 2: УПД document type in document upload
STEPS:
1. Navigate to any entity with document upload capability (e.g., a quote or invoice detail page)
2. Look for a document upload form or "Загрузить документ" button
3. In the document type dropdown, verify "УПД" is available as an option
4. Try uploading a test document with type "УПД"
5. Verify the document appears in the list with correct type label
6. Check console for errors

TEST 3: Document counts on invoice list
STEPS:
1. Navigate to a quote's procurement tab (where invoices are listed)
2. Look for document count badges/indicators next to each invoice
3. Verify counts reflect actual attached documents
4. Check console for errors

EXPECT:
- "Цепочка документов" tab visible on quote detail pages
- Documents grouped by type in correct order: КП → Спецификация → Инвойс → ГТД → УПД
- УПД available as document type for upload
- Document counts shown on invoice list
- No console errors
- No regression on existing document functionality

REPORT_TO: .claude/test-ui-reports/report-20260210-1750-p26-p210.md
