BROWSER TEST
timestamp: 2026-02-09T20:45:00
session: 2026-02-09 #8
base_url: https://kvotaflow.ru

TASK: [86af78d8m] Spec Control — search + counter OOB (re-test with deployed code)
URL: /dashboard?tab=spec-control
STEPS:
1. Navigate to /dashboard?tab=spec-control
2. Verify unified table loads with specs
3. Note the "Записей: N" counter value at bottom of table (should match total rows)
4. Click chip "Черновики" — verify table filters to drafts only
5. Check "Записей: N" counter — should UPDATE to match filtered count (was stuck at total before)
6. Click "Все" — verify all rows return, counter resets to total
7. Type a customer name fragment in search input
8. Wait 500ms, verify table filters to matching rows
9. Check "Записей: N" counter updates to match search results
10. Clear search, verify all rows return
11. Check console for errors
EXPECT: Search filters table, counter updates on every filter/search via OOB swap, no errors

TASK: [86af6ykhh] Deals table + Payments (first clean test — DB was down before)
URL: /finance → deals list → deal detail → ПЛАТЕЖИ
STEPS:
1. Navigate to /finance
2. Verify finance page loads with summary cards
3. Look for deals table/list below summary cards — should now render (company_name query fix)
4. Click on a deal row to open deal detail page
5. On deal detail page, scroll down to find "ПЛАТЕЖИ" section
6. Verify payments section exists with header "ПЛАТЕЖИ"
7. Look for "Добавить платёж" form or button
8. If form exists, fill: select payment mode, enter amount (e.g. 1000), enter today's date
9. Submit the payment form
10. Verify payment appears in the list
11. Check console for errors
EXPECT: Deals table renders, deal detail accessible, ПЛАТЕЖИ section visible, payment CRUD works

TASK: [86af6ykc7] Brands inline delete — two-click confirm (new behavior)
URL: /suppliers → any supplier → Бренды tab
STEPS:
1. Navigate to /suppliers, click any supplier to open detail
2. Click "Бренды" tab
3. Add a test brand "TestInlineDelete" via the input + button
4. Verify brand appears in list
5. Click "Удалить" button — should change text to "Точно?" (NO browser confirm dialog)
6. Click "Точно?" — should delete the brand via htmx.ajax
7. Verify brand removed from list
8. Check console for errors
EXPECT: Delete uses inline two-click (no browser dialog), brand removed cleanly, no errors

REPORT_TO: .claude/test-ui-reports/report-20260209-2045.md
