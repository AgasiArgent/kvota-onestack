BROWSER TEST
timestamp: 2026-02-10T12:20:00
session: 2026-02-09 #8 (continued)
base_url: https://kvotaflow.ru

TASK: [86af78d8m] Spec Control — search + counter + chip filters (re-test after HTMX fix)
URL: /dashboard?tab=spec-control
STEPS:
1. Navigate to /dashboard?tab=spec-control
2. Verify unified table loads with specs (columns: #, НОМЕР, КЛИЕНТ, СТАТУС, ВАЛЮТА, СУММА, ПРОФИТ, ДАТА)
3. Note the "Записей: N" counter value at bottom of table
4. Click chip "Черновики" — verify table filters to drafts only (HTMX swap, NOT full page reload)
5. Check "Записей: N" counter — should UPDATE to match filtered count (client-side update via htmx:afterSwap)
6. Click "Все" — verify all rows return, counter resets to total
7. Type a customer name fragment in search input (use first few letters of any visible customer)
8. Wait 500ms, verify table filters to matching rows (HTMX swap, NOT page reload)
9. Check "Записей: N" counter updates to match search results
10. Clear search, verify all rows return
11. Click one of the stat cards at top (e.g. "Ожидают спецификации") — verify table filters
12. Check console for errors
EXPECT: Search filters table via HTMX, chips filter via HTMX, counter updates on every filter/search, no full page reloads, no errors

TASK: [86af6ykhh] Deals table + Payments (re-test after query fix)
URL: /finance
STEPS:
1. Navigate to /finance
2. Verify finance page loads with summary cards (Активные/Завершённые/Отменённые)
3. Look for deals table below summary cards — should now render (customer_name column removed from query)
4. Verify deal rows show: номер, клиент, сумма, статус, дата
5. Click on a deal row to open deal detail page
6. On deal detail page, scroll down to find "ПЛАТЕЖИ" section
7. Verify payments section exists with header "ПЛАТЕЖИ"
8. Look for "Добавить платёж" form
9. If form exists, fill: select payment mode (incoming/outgoing), enter amount (e.g. 1000), enter today's date
10. Submit the payment form
11. Verify payment appears in the list
12. Check console for errors
EXPECT: Deals table renders with rows, deal detail accessible, ПЛАТЕЖИ section visible, payment form works

TASK: [86af6ykc7] Brands inline delete — confirm still works (sanity check)
URL: /suppliers → any supplier → Бренды tab
STEPS:
1. Navigate to /suppliers, click any supplier to open detail
2. Click "Бренды" tab
3. Add a test brand "SanityCheck" via the input + button
4. Verify brand appears in list
5. Click "Удалить" button — should change text to "Точно?" (NO browser confirm dialog)
6. Click "Точно?" — should delete the brand
7. Verify brand removed from list
8. Check console for errors
EXPECT: Delete uses inline two-click (no browser dialog), brand removed cleanly, no errors

REPORT_TO: .claude/test-ui-reports/report-20260210-1220.md
