BROWSER TEST
timestamp: 2026-03-11T13:00:00+03:00
session: 2026-03-11 #3
base_url: https://kvotaflow.ru

TASK: B3 — Margin/markup formula alignment verification
URL: https://kvotaflow.ru/quotes
STEPS:
1. Find a CALCULATED quote (status На рассмотрении or Одобрена) — e.g. Q-202603-0015
2. Open quote → Обзор tab → read ИТОГО card
3. Check "Маржа (профит ÷ выручка без НДС)" label exists with a % value (not "—")
4. Check "Наценка (профит ÷ себестоимость)" label exists with a % value (not "—")
5. Open Расчет tab → Анализ стоимости section
6. Compare margin % from ИТОГО with Cost Analysis margin — they should now be close/aligned
7. Check waterfall cards: "Чистая прибыль" card should show amount, "Наценка (выр. ÷ закуп − 1)" card should show %
8. Navigate to Продажи tab → check ИТОГО card has same formula hint labels
EXPECT: Margin % consistent between Overview ИТОГО and Cost Analysis. Formula hints visible on all labels. No "—" for margin/markup on calculated quotes.
NOTE: Quotes calculated BEFORE this deploy may show "—" for margin (missing revenue_no_vat column). If so, recalculate one quote and re-check.

TASK: A4 #25 — Contact duplicate prevention
URL: https://kvotaflow.ru/customers
STEPS:
1. Navigate to any customer detail page → Контакты tab
2. Note an existing contact name (e.g. "Director dir" or any existing contact)
3. Click "Добавить контакт"
4. Enter the SAME name as the existing contact
5. Fill position and submit
6. Verify error message appears: "Контакт «Name» уже существует у данного клиента"
7. Verify no duplicate was created (check contact list)
8. Now create a contact with a DIFFERENT name — verify it succeeds normally
EXPECT: Duplicate blocked with clear message. New unique contacts still work.

REPORT_TO: .claude/test-ui-reports/report-20260311-1300.md
