BROWSER TEST
timestamp: 2026-03-11T16:33:00+03:00
session: 2026-03-11 #4
base_url: https://kvotaflow.ru

TASK: B3 retest — Margin/markup with fallback on old quotes
URL: https://kvotaflow.ru/quotes
STEPS:
1. Find a CALCULATED quote (e.g. Q-202603-0015, status На рассмотрении)
2. Open quote → Продажи tab → read ИТОГО card
3. Check "Маржа (профит ÷ выр. без НДС)" — should show a % value, NOT "—"
4. Check "Наценка (профит ÷ себест.)" — should show a % value, NOT "—"
5. Open Сводка tab → read ИТОГО card (Block VI)
6. Check "Маржа (профит ÷ выручка без НДС)" — should show same % as Продажи
7. Check "Наценка (профит ÷ себестоимость)" — should show same % as Продажи
8. Open Расчет tab → Анализ стоимости → compare margin % with ИТОГО values
9. Values should be close (may differ slightly due to fallback rounding vs engine precision)
EXPECT: No "—" for margin or markup on calculated quotes. All three tabs show consistent values.

REPORT_TO: .claude/test-ui-reports/report-20260311-1330.md
