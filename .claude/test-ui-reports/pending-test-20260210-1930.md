BROWSER TEST
timestamp: 2026-02-10T19:30:00+03:00
session: 2026-02-10 #1
base_url: https://kvotaflow.ru

TASK: [BUG-A3 DEBUG] Spec form save — debug markers deployed
URL: /spec-control/36801d1b-5785-476e-80dc-fc18610f64a2
IMPORTANT: Wait 2 minutes after seeing this file for deploy to complete (commit 88bf051).

STEPS:
1. First reset the spec to draft: navigate to the spec page
2. If status is NOT "Черновик", use admin dropdown to set it to "Черновик"
3. Wait for page to reload with editable fields
4. Select a contract from "Договор" dropdown (any contract)
5. Enter 44 in "Срок поставки (дней)"
6. Click "Сохранить"
7. After page reloads, note what contract and delivery_days show (persist or empty?)
8. CRITICAL — check server logs:
   Run: ssh beget-kvota "docker logs kvota-onestack --tail 30 2>&1 | grep DEBUG"
   Copy ALL lines starting with [DEBUG] into the report
9. ALSO check database directly:
   Run: ssh beget-kvota "docker exec supabase-db psql -U postgres -d postgres -c \"SELECT delivery_days, contract_id, specification_number FROM kvota.specifications WHERE id = '36801d1b-5785-476e-80dc-fc18610f64a2';\""
   If delivery_days = 777 → save path runs (form issue)
   If delivery_days = 44 → save works fully (display issue)
   If delivery_days = NULL → save path never reached (routing issue)

EXPECT:
- Capture [DEBUG spec-save] and [DEBUG spec-save UPDATE] log lines
- Capture DB values
- These tell us exactly where the bug is

REPORT_TO: .claude/test-ui-reports/report-20260210-1930.md
